(ns petra.core
  (:require [petra.parser :as parser]
            [petra.dungeon :as dungeon]
            [petra.engine :as engine]))

(defn main-loop []
  ;TODO
  (engine/tell! "Nothing to see here.")
  )